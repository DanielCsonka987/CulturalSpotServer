const ws = require('ws');

const { startTestingServer, exitTestingServer } = require('../server')

const client = new ws('ws://localhost:4040/feedback');


beforeAll(()=>{

})

it('Stg', (done)=>{
  done()
})